<!-- @format -->

# Página Principal Personal Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convertir la home en una página personal: nombre como título, avatar placeholder, descripción y sección de posts destacados (`featured`) con fallback a los últimos 3.

**Architecture:** Extiende el esquema de Content Collections con un flag `featured`, añade una utilidad de selección con fallback, crea el componente `ProfileHero` y rediseña `index.astro`. Sin cambios en BaseLayout ni en las demás páginas.

**Tech Stack:** Astro 5 Content Collections (zod), TypeScript strict, CSS existente.

**Spec:** `docs/superpowers/specs/2026-08-22-home-page-design.md`

**Nota sobre verificación:** Igual que el proyecto base: `npm run build` exitoso + inspección del HTML en `dist/` con `grep`.

---

### Task 1: Flag featured en el esquema + utilidad getFeaturedPosts

**Files:**

- Modify: `src/content.config.ts`
- Modify: `src/utils/posts.ts`

- [ ] **Step 1: Añadir `featured` al esquema**

En `src/content.config.ts`, dentro de `z.object({...})`, después de la línea `draft: z.boolean().default(false),`, añadir:

```ts
    featured: z.boolean().default(false),
```

El objeto completo debe quedar así:

```ts
  schema: z.object({
    title: z.string().min(1),
    description: z.string().min(1),
    pubDate: z.coerce.date(),
    updatedDate: z.coerce.date().optional(),
    tags: z.array(z.string()).default([]),
    draft: z.boolean().default(false),
    featured: z.boolean().default(false),
  }),
```

- [ ] **Step 2: Añadir `getFeaturedPosts` a `src/utils/posts.ts`**

Al final del archivo, añadir:

```ts
/** Devuelve hasta `limit` posts marcados como destacados, por fecha descendente.
 * Si no hay ninguno destacado, devuelve los últimos `limit` posts publicados. */
export async function getFeaturedPosts(
  limit = 3
): Promise<CollectionEntry<'blog'>[]> {
  const posts = await getPublishedPosts()
  const featured = posts.filter(post => post.data.featured)
  return (featured.length > 0 ? featured : posts).slice(0, limit)
}
```

- [ ] **Step 3: Verificar generación de tipos**

Run: `npx astro sync`
Expected: termina sin errores.

- [ ] **Step 4: Commit**

```bash
git add src/content.config.ts src/utils/posts.ts
git commit -m "feat: flag featured y utilidad de posts destacados"
```

---

### Task 2: SITE.author, componente ProfileHero y estilos

**Files:**

- Modify: `src/consts.ts`
- Create: `src/components/ProfileHero.astro`
- Modify: `src/styles/global.css`

- [ ] **Step 1: Actualizar nombre del autor en `src/consts.ts`**

El archivo completo debe quedar así:

```ts
export const SITE = {
  url: 'https://thoughts.miguelintech.com',
  title: 'Thoughts',
  description:
    'Bienvenido a un pequeño rincón de mi mente donde comparto todo aquello que me da curiosidad y lo que pienso al respecto..',
  author: 'Miguel Bastidas',
  locale: 'es'
}
```

Efecto esperado: el footer pasará a mostrar "© {year} Miguel Bastidas" automáticamente (usa `SITE.author`).

- [ ] **Step 2: Crear `src/components/ProfileHero.astro`**

```astro
---
import { SITE } from '../consts';
---

<section class="profile-hero">
  <div class="profile-avatar">
    <svg viewBox="0 0 96 96" width="96" height="96" aria-hidden="true">
      <circle cx="48" cy="48" r="48" fill="#1d4ed8"></circle>
      <text x="48" y="62" font-family="system-ui, sans-serif" font-size="40" font-weight="700" fill="#ffffff" text-anchor="middle">M</text>
    </svg>
  </div>
  <div class="profile-info">
    <h1>{SITE.author}</h1>
    <p>{SITE.description}</p>
  </div>
</section>
```

Nota: cuando haya foto real, se reemplaza el `<svg>` por `<img src="/perfil.jpg" alt="Foto de {SITE.author}" width="96" height="96" />`.

- [ ] **Step 3: Reemplazar estilos `.hero` por `.profile-hero` en `src/styles/global.css`**

Eliminar estas dos reglas:

```css
.hero {
  padding-block: 3rem 2rem;
}
.hero h1 {
  margin-bottom: 0.25rem;
}
```

Y en su lugar añadir:

```css
.profile-hero {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  padding-block: 3rem 2rem;
}

.profile-avatar svg {
  display: block;
  border-radius: 50%;
}

.profile-info h1 {
  margin-bottom: 0.25rem;
}

.featured-heading {
  font-size: 1.25rem;
  margin-block: 0 0.5rem;
}

@media (max-width: 480px) {
  .profile-hero {
    flex-direction: column;
    text-align: center;
  }
}
```

- [ ] **Step 4: Commit**

```bash
git add src/consts.ts src/components/ProfileHero.astro src/styles/global.css
git commit -m "feat: componente de perfil personal con avatar placeholder"
```

---

### Task 3: Rediseño de la home

**Files:**

- Modify: `src/pages/index.astro` (reescritura completa)
- Modify: `src/content/blog/mi-primer-post.md`

- [ ] **Step 1: Reescribir `src/pages/index.astro`**

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import PostCard from '../components/PostCard.astro';
import ProfileHero from '../components/ProfileHero.astro';
import { SITE } from '../consts';
import { getFeaturedPosts } from '../utils/posts';

const posts = await getFeaturedPosts(3);
---

<BaseLayout title={`${SITE.author} · ${SITE.title}`} description={SITE.description}>
  <ProfileHero />

  <section aria-label="Posts destacados">
    <h2 class="featured-heading">Posts destacados</h2>
    {posts.map((post) => <PostCard post={post} />)}
  </section>

  <p><a href="/blog/">Ver todos los posts →</a></p>
</BaseLayout>
```

- [ ] **Step 2: Marcar un post como destacado**

En `src/content/blog/mi-primer-post.md`, añadir `featured: true` al frontmatter. Debe quedar así:

```markdown
---
title: 'Mi primer post en el blog'
description: 'Estreno el blog explicando por qué lo creé con Astro y qué quiero escribir aquí.'
pubDate: 2026-08-20
tags: ['general']
featured: true
---
```

- [ ] **Step 3: Verificar build y HTML generado**

Run: `npm run build`
Expected: build exitoso.

Run: `grep -o '<title>[^<]*</title>' dist/index.html`
Expected: `<title>Miguel Bastidas · Thoughts</title>`

Run: `grep -o '<h1>[^<]*</h1>' dist/index.html`
Expected: `<h1>Miguel Bastidas</h1>`

Run: `grep -c 'profile-hero' dist/index.html`
Expected: `1`

Run: `grep -o '<article class="post-card">' dist/index.html | wc -l`
Expected: `1` (solo el post destacado; con 1 featured no hay fallback).

Run: `grep -o 'Posts destacados' dist/index.html | wc -l`
Expected: `1`

Run: `grep -o '<h1>[^<]*</h1>' dist/blog/index.html`
Expected: sigue siendo `Todos los posts` (las otras páginas no cambian).

- [ ] **Step 4: Commit**

```bash
git add src/pages/index.astro src/content/blog/mi-primer-post.md
git commit -m "feat: home personal con perfil y posts destacados"
```

---

### Task 4: Verificación del fallback y documentación

**Files:**

- Modify: `README.md`
- Modify: `src/content/blog/mi-primer-post.md` (temporal para probar, se restaura)

- [ ] **Step 1: Probar el fallback quitando el flag**

Edita temporalmente `src/content/blog/mi-primer-post.md` y elimina la línea `featured: true`.

Run: `npm run build`
Expected: build exitoso.

Run: `grep -o '<article class="post-card">' dist/index.html | wc -l`
Expected: `2` (fallback a los últimos publicados; hay 2 posts publicados en total).

Restaura la línea `featured: true` en el frontmatter (estado final: el post queda destacado).

- [ ] **Step 2: Verificación final tras restaurar**

Run: `npm run build`
Expected: build exitoso.

Run: `grep -o '<article class="post-card">' dist/index.html | wc -l`
Expected: `1`

Run:

```bash
find dist -name '*.html' | wc -l
grep -rl 'og:title' dist --include='*.html' | wc -l
```

Expected: los dos números son iguales (todos los HTML conservan sus metadatos OG).

- [ ] **Step 3: Documentar el flag en `README.md`**

En la sección "Escribir un post", dentro del bloque yaml de ejemplo, añadir esta línea después de `draft: true ...`:

```yaml
featured: false # opcional: aparece en la portada
```

El bloque de ejemplo debe quedar así:

```yaml
---
title: 'Título del post' # obligatorio
description: 'Resumen para buscadores' # obligatorio
pubDate: 2026-08-22 # obligatorio
updatedDate: 2026-08-23 # opcional
tags: ['astro', 'seo'] # opcional (minúsculas, sin espacios)
draft: true # opcional: no se publica en producción
featured: false # opcional: aparece en la portada
---
Contenido en Markdown...
```

- [ ] **Step 4: Commit**

```bash
git add README.md src/content/blog/mi-primer-post.md
git commit -m "docs: documenta el flag featured en el README"
```

---

## Fin del plan

Estado final esperado: home con perfil personal ("Miguel Bastidas", avatar placeholder, descripción), sección "Posts destacados" mostrando el post marcado como featured, fallback verificado, y todas las páginas manteniendo sus metadatos SEO.
